# Unsupported historical archive

Everything here is inert forensic material. Archived programs use `.txt`
suffixes, are excluded from all packages and workflows, and are not supported
entrypoints. Do not execute, source, copy into a payload, or use for recovery.

The only supported implementation is the Python package under `rebuild/`.
